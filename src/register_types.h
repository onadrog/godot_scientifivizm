#pragma once

#include <godot_cpp/core/class_db.hpp>

using namespace godot;

void initialize_supa_cam(ModuleInitializationLevel p_level);
void uninitialize_supa_cam(ModuleInitializationLevel p_level);
